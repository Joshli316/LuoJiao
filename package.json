{
  "name": "luojiao",
  "version": "1.0.0",
  "description": "Map-based cultural navigation for Chinese students in LA",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "typescript": "^6.0.2"
  }
}
